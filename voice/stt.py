"""STT text processing (deprecated).

This module was used for state-based text post-processing (wake-word
stripping in LISTENING state, pass-through in AWAKE). It is no longer
needed because Sherpa-ONNX KWS handles wake-word detection separately
from STT transcription.
"""